
// Include the headers that contain the definitions of the sync types you want to add.
#include "InstancedStruct.h"

// Define this macro as a comma-separated list (no trailing comma) of the names of your sync types. You can choose any name you want here, but make sure to use
// the same names throughout.
#define TYPE_NAMES InstancedStruct

// Define this macro as a list (no commas) of DEFINE_SYNC_TYPE(<Name>, <Type>) macros.
#define TYPE_DEFS DEFINE_SYNC_TYPE(InstancedStruct, ::FInstancedStruct)
